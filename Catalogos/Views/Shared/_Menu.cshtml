@using Microsoft.AspNet.Identity
<nav class="navbar navbar-default navbar-fixed-top sidebar" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-sidebar-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse" id="bs-sidebar-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li class="active">
                    <a href="@Url.Action("Index","Welcome")">Inicio
                    <span style="font-size:16px;" class="glyphicon glyphicon-home"></span>
                    </a>
                </li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Catálogos 
                        <span class="caret"></span><span style="font-size:16px;" ></span>
                    </a>
                    <ul class="dropdown-menu forAnimate" role="menu">
                        <li><a href="@Url.Action("Index", "Usuarios", new RouteValueDictionary(new { context = "Catálogo Usuarios" }))">Usuarios</a></li>                        
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Index", "Roles", new RouteValueDictionary(new { context = "Catálogo Roles" }))">Roles</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Index", "Estados", new RouteValueDictionary(new { context = "Catálogo Estados" }))">Estados</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Index", "Municipios", new RouteValueDictionary(new { context = "Catálogo Municipios" }))">Municipios</a></li>
                        <li class="divider"></li>
                        <li><a href="@Url.Action("Index", "Colonias", new RouteValueDictionary(new { context = "Catálogo Colonias" }))">Colonias</a></li>                        
                    </ul>
                </li>
                <li><a href="#">Expedientes<span style="font-size:16px;" class="pull-right hidden-xs showopacity"></span></a></li>                
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a href="#">Buen día @User.Identity.GetUserName() </a>
                </li>
                <li>@Html.ActionLink("Salir", "Logout", "Home")</li>
            </ul>
        </div>
    </div>
</nav>